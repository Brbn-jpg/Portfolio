---
// Shared section heading for project-detail bodies. Pulled out so the class
// string (repeated 20+ times across en/ and, later, pl/) can't drift between
// locales — change it once here and every project page picks it up.
---
<h3 class="text-2xl font-bold text-slate-200 pt-4 !mt-10 border-t border-slate-800">
  <slot />
</h3>
